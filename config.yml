# The location of the raw (RGB) TIFF segmentations
# I just copied these from OneDrive
binary_img_dir: "binary_imgs/"

# This directory will be created if needed
# Used for storing pre-processed (greyscale) segmentations
processed_img_dir: "greyscale_cache/"
